<h2> Category: <%= category.name %> </h1>

  <% category.projects.forEach(project => { %>
    <div>
      <h4>
        <a href="/projects/<%=project.id%>"><%= project.name %></a>
      </h4>
    </div>  
  <% }) %> 
