<h2>My Projects</h2>
<hr>

<% if (categories.length < 1) { %>
  <h4>Better get coding, I've no projects!</h4>
<% } %>

<% categories.forEach((c) => { %>
  <div class="well">
    <h2>
      <a href="/categories/<%= c.id %>"><%= c.name %></a>
    </h2>
  </div>
<% }); %>


